language: python
python:
  - "3.6"
  - "3.6-dev"  # 3.6 development branch
  - "3.7-dev"  # 3.7 development branch
# command to install dependencies
install:
  - pip install requests
  - pip install json
# command to run tests
script:
  - python -m unittest Homework04a.py
